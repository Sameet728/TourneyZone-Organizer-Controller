<nav class="fixed w-full z-50 top-0 start-0 border-b border-white/20 bg-white/80 backdrop-blur-xl transition-all duration-300" id="mainNavbar">
  <div class="max-w-screen-xl flex flex-wrap items-center justify-between mx-auto p-4">
    
    <a href="/" class="flex items-center space-x-2 rtl:space-x-reverse group">
      <div class="w-10 h-10 bg-brand-600 text-white rounded-xl flex items-center justify-center text-xl shadow-lg shadow-brand-500/30 group-hover:scale-110 transition-transform">
        <i class="fas fa-bolt"></i>
      </div>
      <span class="self-center text-2xl font-extrabold whitespace-nowrap text-slate-900 tracking-tight">
        Tourney<span class="text-brand-600">Zone</span>
      </span>
    </a>

    <button data-collapse-toggle="navbar-sticky" type="button" class="inline-flex items-center p-2 w-10 h-10 justify-center text-sm text-slate-500 rounded-lg md:hidden hover:bg-slate-100 focus:outline-none focus:ring-2 focus:ring-slate-200" aria-controls="navbar-sticky" aria-expanded="false" onclick="toggleMobileMenu()">
      <span class="sr-only">Open main menu</span>
      <i class="fas fa-bars text-xl"></i>
    </button>

    <div class="items-center justify-between hidden w-full md:flex md:w-auto md:order-1" id="navbar-sticky">
      <ul class="flex flex-col p-4 md:p-0 mt-4 font-medium border border-slate-100 rounded-2xl bg-slate-50 md:space-x-8 rtl:space-x-reverse md:flex-row md:mt-0 md:border-0 md:bg-transparent">
        
        <li>
          <a href="/player/tournaments" class="block py-2 px-3 text-slate-700 rounded hover:bg-slate-100 md:hover:bg-transparent md:hover:text-brand-600 md:p-0 transition-colors">
            Tournaments
          </a>
        </li>

        <% if (!currentUser) { %>
          <li class="md:hidden border-t border-slate-200 my-2 pt-2"></li>
          <li class="flex flex-col md:flex-row gap-2">
            <a href="/login" class="px-5 py-2 text-sm font-bold text-slate-700 bg-white border border-slate-200 rounded-xl hover:bg-slate-50 hover:text-brand-600 transition-all text-center">
              Log In
            </a>
            <a href="/signup" class="px-5 py-2 text-sm font-bold text-white bg-slate-900 rounded-xl hover:bg-brand-600 shadow-lg hover:shadow-brand-500/30 transition-all text-center">
              Sign Up
            </a>
          </li>
        <% } else { %>
          
          <% if (currentUser.role === 'player') { %>
            <li>
              <a href="/player/my-tournaments" class="block py-2 px-3 text-slate-700 rounded hover:bg-slate-100 md:hover:bg-transparent md:hover:text-brand-600 md:p-0 transition-colors">
                My Matches
                <% if (currentUser.tournamentsJoined?.length > 0) { %>
                  <span class="inline-flex items-center justify-center w-5 h-5 ms-2 text-xs font-semibold text-white bg-red-500 rounded-full">
                    <%= currentUser.tournamentsJoined.length %>
                  </span>
                <% } %>
              </a>
            </li>
          <% } %>

          <% if (currentUser.role === 'organizer') { %>
            <li>
              <a href="/organizer/tournaments/new" class="block py-2 px-3 text-slate-700 rounded hover:bg-slate-100 md:hover:bg-transparent md:hover:text-brand-600 md:p-0 transition-colors">
                <i class="fas fa-plus-circle mr-1"></i> Host Event
              </a>
            </li>
          <% } %>

          <li class="md:ml-4 mt-4 md:mt-0">
            <div class="relative group">
              <a href="/<%= currentUser.role %>" class="flex items-center gap-2 px-4 py-2 bg-slate-100 text-slate-900 rounded-xl hover:bg-slate-200 transition-colors font-bold text-sm w-full justify-center md:w-auto">
                <div class="w-6 h-6 bg-brand-500 text-white rounded-full flex items-center justify-center text-xs">
                  <i class="fas fa-user"></i>
                </div>
                <span>Dashboard</span>
              </a>
            </div>
          </li>

          <li class="md:hidden">
            <a href="/logout" class="block py-2 px-3 text-red-600 rounded hover:bg-red-50 font-bold text-center mt-2">
              Log Out
            </a>
          </li>
          
          <li class="hidden md:block">
            <a href="/logout" class="text-slate-400 hover:text-red-500 transition-colors" title="Logout">
              <i class="fas fa-sign-out-alt"></i>
            </a>
          </li>

        <% } %>
      </ul>
    </div>
  </div>
</nav>

<div class="h-20"></div>

<script>
  function toggleMobileMenu() {
    const menu = document.getElementById('navbar-sticky');
    menu.classList.toggle('hidden');
  }
</script>