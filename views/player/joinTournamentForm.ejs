<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Join <%= tournament.name %></title>
  
  <script src="https://cdn.tailwindcss.com"></script>
  
  <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" />

  <script>
    tailwind.config = {
      theme: {
        extend: {
          fontFamily: {
            sans: ['"Plus Jakarta Sans"', 'sans-serif'],
          },
          colors: {
            brand: {
              500: '#3b82f6',
              600: '#2563eb',
              900: '#1e3a8a',
            }
          }
        }
      }
    }
  </script>

  <style>
    @keyframes blob {
      0% { transform: translate(0px, 0px) scale(1); }
      33% { transform: translate(30px, -50px) scale(1.1); }
      66% { transform: translate(-20px, 20px) scale(0.9); }
      100% { transform: translate(0px, 0px) scale(1); }
    }
    .animate-blob { animation: blob 7s infinite; }
    .animation-delay-2000 { animation-delay: 2s; }
  </style>
</head>

<body class="bg-slate-50 text-slate-800 font-sans min-h-screen flex flex-col relative">

  <div class="fixed top-0 left-0 w-full h-full overflow-hidden -z-10 pointer-events-none">
    <div class="absolute top-0 right-1/4 w-96 h-96 bg-blue-100 rounded-full mix-blend-multiply filter blur-3xl opacity-40 animate-blob"></div>
    <div class="absolute bottom-0 left-1/4 w-96 h-96 bg-purple-100 rounded-full mix-blend-multiply filter blur-3xl opacity-40 animate-blob animation-delay-2000"></div>
  </div>

  <div class="relative z-50">
    <%- include("../includes/navbar") %>
  </div>

  <div class="container mx-auto px-4 py-8 lg:py-12 flex-grow flex items-center justify-center">
    
    <div class="w-full max-w-lg relative">
      
      <div class="bg-white/80 backdrop-blur-xl border border-white/20 shadow-2xl rounded-3xl overflow-hidden">
        
        <div class="bg-gradient-to-r from-brand-600 to-indigo-600 p-6 text-center text-white">
          <h3 class="text-xl font-bold uppercase tracking-wider opacity-80 mb-1">Registration</h3>
          <h1 class="text-3xl font-extrabold"><%= tournament.name %></h1>
          <div class="mt-4 inline-flex items-center bg-white/20 backdrop-blur-md rounded-full px-4 py-1 border border-white/30">
            <span class="text-sm font-bold">Entry Fee: ₹<%= tournament.entryFee %></span>
          </div>
        </div>

        <div class="p-6 md:p-8">
          
          <% if (tournament.teamLimit && tournament.teams.length >= tournament.teamLimit) { %>
            
            <div class="text-center py-8">
              <div class="w-20 h-20 bg-red-50 text-red-500 rounded-full flex items-center justify-center mx-auto mb-4 text-3xl">
                <i class="fas fa-ban"></i>
              </div>
              <h3 class="text-2xl font-bold text-slate-900 mb-2">Registration Closed</h3>
              <p class="text-slate-500">All slots for this tournament have been filled. Better luck next time!</p>
            </div>

          <% } else if (!registration) { %>

            <form method="POST" action="/player/tournaments/<%= tournament._id %>/register" class="space-y-6">

              <div class="space-y-4">
                <h4 class="text-sm font-bold text-slate-400 uppercase tracking-wider border-b border-slate-100 pb-2">
                  <i class="fas fa-users text-brand-500 mr-2"></i> Team Details
                </h4>

                <div>
                  <label class="block text-sm font-bold text-slate-700 ml-1 mb-1">Team Name <span class="text-red-500">*</span></label>
                  <input name="teamName" required placeholder="e.g. Delta Force"
                    class="w-full px-4 py-3 bg-slate-50 border border-slate-200 rounded-xl focus:bg-white focus:ring-2 focus:ring-brand-500 outline-none transition-all font-bold text-slate-800">
                </div>

                <div>
                  <label class="block text-sm font-bold text-slate-700 ml-1 mb-1">Team Leader</label>
                  <div class="w-full px-4 py-3 bg-slate-100 border border-slate-200 rounded-xl text-slate-500 font-medium flex items-center justify-between">
                    <span><%= user.username %></span>
                    <i class="fas fa-crown text-yellow-500"></i>
                  </div>
                </div>

                <div class="bg-slate-50 p-4 rounded-xl border border-slate-100">
                  <label class="block text-sm font-bold text-slate-700 mb-3">Teammates (Optional)</label>
                  <div class="space-y-3">
                    <% for (let i = 2; i <= 4; i++) { %>
                      <div class="relative">
                        <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none text-slate-400">
                          <span class="text-xs font-bold">#<%= i %></span>
                        </div>
                        <input name="member<%= i %>" placeholder="Player ID / Name"
                          class="w-full pl-10 pr-4 py-2 bg-white border border-slate-200 rounded-lg focus:ring-2 focus:ring-brand-500 outline-none text-sm">
                      </div>
                    <% } %>
                  </div>
                </div>
              </div>

              <div class="pt-2 space-y-4">
                <h4 class="text-sm font-bold text-slate-400 uppercase tracking-wider border-b border-slate-100 pb-2">
                  <i class="fas fa-wallet text-brand-500 mr-2"></i> Payment (UPI)
                </h4>

                <% if (tournament.organizer?.upiId) { 
                    const upiId = tournament.organizer.upiId;
                    const amount = tournament.entryFee;
                    const note = tournament.name;
                    const upiLink = `upi://pay?pa=${upiId}&pn=Organizer&am=${amount}&cu=INR&tn=${encodeURIComponent(note)}`;
                %>

                  <div class="bg-blue-50 border border-blue-100 rounded-2xl p-6 text-center">
                    <p class="text-blue-800 font-bold mb-4 flex items-center justify-center gap-2">
                      <span>Scan to Pay</span> 
                      <span class="text-2xl text-slate-900 font-extrabold">₹<%= amount %></span>
                    </p>
                    
                    <div class="bg-white p-3 rounded-xl shadow-sm inline-block border border-slate-200 mb-4">
                      <img
                        src="https://api.qrserver.com/v1/create-qr-code/?size=220x220&data=<%= encodeURIComponent(upiLink) %>"
                        alt="UPI QR Code"
                        class="rounded-lg mix-blend-multiply"
                        width="180" height="180"
                      />
                    </div>
                    
                    <div class="text-xs text-blue-600 bg-blue-100/50 py-2 px-3 rounded-lg mx-auto max-w-xs">
                      <i class="fas fa-exclamation-circle mr-1"></i> 
                      IMPORTANT: Copy the <strong>UTR / Transaction ID</strong> after paying. Incorrect UTR results in rejection.
                    </div>
                  </div>

                <% } else { %>
                  <div class="bg-red-50 border border-red-200 rounded-xl p-4 text-center text-red-600">
                    <i class="fas fa-times-circle text-2xl mb-2"></i>
                    <p class="font-bold">Payment Setup Incomplete</p>
                    <p class="text-sm">The organizer has not added UPI details yet.</p>
                  </div>
                <% } %>

                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                  <div>
                    <label class="block text-sm font-bold text-slate-700 ml-1 mb-1">Payer Name <span class="text-red-500">*</span></label>
                    <input name="payerName" required placeholder="Name on UPI App"
                      class="w-full px-4 py-3 bg-slate-50 border border-slate-200 rounded-xl focus:bg-white focus:ring-2 focus:ring-brand-500 outline-none transition-all font-medium text-slate-800">
                  </div>
                  <div>
                    <label class="block text-sm font-bold text-slate-700 ml-1 mb-1">UTR / Ref No. <span class="text-red-500">*</span></label>
                    <input name="transactionId" required placeholder="e.g. 3245xxxx"
                      class="w-full px-4 py-3 bg-slate-50 border border-slate-200 rounded-xl focus:bg-white focus:ring-2 focus:ring-brand-500 outline-none transition-all font-medium text-slate-800">
                  </div>
                </div>
                
                <input name="amount" type="hidden" value="<%= tournament.entryFee %>">

                <button type="submit" <%= !tournament.organizer?.upiId ? "disabled" : "" %>
                  class="w-full group relative flex justify-center py-4 px-4 border border-transparent text-lg font-bold rounded-xl text-white bg-slate-900 hover:bg-brand-600 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-slate-900 transition-all duration-200 shadow-lg hover:shadow-brand-500/30 hover:-translate-y-1 disabled:opacity-50 disabled:cursor-not-allowed">
                  <span class="mr-2">🚀</span> Submit & Request Slot
                </button>

                <p class="text-center text-xs text-slate-400 mt-2">
                  Your slot is confirmed only after the organizer approves your payment.
                </p>

              </div>
            </form>

          <% } else if (registration.status === "pending") { %>

            <div class="text-center py-10">
              <div class="relative w-24 h-24 mx-auto mb-6">
                 <div class="absolute inset-0 bg-amber-100 rounded-full animate-ping opacity-75"></div>
                 <div class="relative w-24 h-24 bg-amber-100 text-amber-500 rounded-full flex items-center justify-center text-4xl shadow-sm">
                   <i class="fas fa-hourglass-half"></i>
                 </div>
              </div>
              <h3 class="text-2xl font-bold text-slate-900 mb-2">Approval Pending</h3>
              <p class="text-slate-500 max-w-xs mx-auto mb-6">Your registration has been sent. The organizer is reviewing your payment details.</p>
              <div class="inline-block bg-slate-50 px-4 py-2 rounded-lg border border-slate-200 text-sm font-mono text-slate-600">
                Team: <%= registration.teamName %>
              </div>
            </div>

          <% } else if (registration.status === "accepted") { %>

            <div class="text-center py-10">
              <div class="w-24 h-24 bg-green-100 text-green-600 rounded-full flex items-center justify-center mx-auto mb-6 text-4xl shadow-sm">
                <i class="fas fa-check-circle"></i>
              </div>
              <h3 class="text-2xl font-bold text-slate-900 mb-2">Registration Accepted!</h3>
              <p class="text-slate-500 max-w-xs mx-auto">You are in! The Room ID and Password will be shared here or via email soon.</p>
            </div>

          <% } else { %>

            <div class="text-center py-10">
              <div class="w-24 h-24 bg-red-100 text-red-500 rounded-full flex items-center justify-center mx-auto mb-6 text-4xl shadow-sm">
                <i class="fas fa-times-circle"></i>
              </div>
              <h3 class="text-2xl font-bold text-slate-900 mb-2">Registration Rejected</h3>
              <p class="text-slate-500 max-w-xs mx-auto mb-6">Unfortunately, your request was not approved.</p>
              
              <div class="bg-red-50 border border-red-100 rounded-xl p-4 text-sm text-red-700 mb-6">
                <strong>Reason:</strong> <%= registration.rejectionReason || "Invalid UTR or Payment Mismatch" %>
              </div>
            </div>

          <% } %>

        </div>
        
        <div class="bg-slate-50 px-6 py-4 border-t border-slate-100 text-center">
          <a href="/player/tournaments" class="text-sm font-bold text-slate-500 hover:text-slate-800 transition-colors">
            <i class="fas fa-arrow-left mr-1"></i> Back to Tournaments
          </a>
        </div>

      </div>
    </div>
  </div>

  <div class="mt-auto">
    <%- include("../includes/footer") %>
  </div>

</body>
</html>