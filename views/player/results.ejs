<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Tournament Archive | TourneyZone</title>
  
  <script src="https://cdn.tailwindcss.com"></script>
  
  <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" />

  <script>
    tailwind.config = {
      theme: {
        extend: {
          fontFamily: {
            sans: ['"Plus Jakarta Sans"', 'sans-serif'],
          },
          colors: {
            brand: {
              500: '#3b82f6',
              600: '#2563eb',
              900: '#1e3a8a',
            }
          }
        }
      }
    }
  </script>

  <style>
    @keyframes blob {
      0% { transform: translate(0px, 0px) scale(1); }
      33% { transform: translate(30px, -50px) scale(1.1); }
      66% { transform: translate(-20px, 20px) scale(0.9); }
      100% { transform: translate(0px, 0px) scale(1); }
    }
    .animate-blob { animation: blob 7s infinite; }
    .animation-delay-2000 { animation-delay: 2s; }
  </style>
</head>

<body class="bg-slate-50 text-slate-800 font-sans min-h-screen flex flex-col relative">

  <div class="fixed top-0 left-0 w-full h-full overflow-hidden -z-10 pointer-events-none">
    <div class="absolute top-0 right-0 w-96 h-96 bg-amber-100 rounded-full mix-blend-multiply filter blur-3xl opacity-40 animate-blob"></div>
    <div class="absolute bottom-0 left-0 w-96 h-96 bg-blue-100 rounded-full mix-blend-multiply filter blur-3xl opacity-40 animate-blob animation-delay-2000"></div>
  </div>

  <div class="relative z-50">
    <%- include("../includes/navbar") %>
  </div>

  <div class="container mx-auto px-4 py-8 lg:py-12 flex-grow">
    
    <div class="text-center mb-12">
      <div class="inline-flex items-center justify-center p-3 bg-white rounded-2xl shadow-sm border border-slate-100 mb-4">
        <i class="fas fa-trophy text-2xl text-yellow-500"></i>
      </div>
      <h1 class="text-3xl md:text-4xl font-extrabold text-slate-900 tracking-tight mb-2">
        Hall of Fame
      </h1>
      <p class="text-slate-500 font-medium max-w-lg mx-auto">
        Browse the archives of past battles and see who claimed the crown.
      </p>
    </div>

    <% if (tournaments.length === 0) { %>
      
      <div class="bg-white/60 backdrop-blur-xl border border-white/20 rounded-3xl p-12 text-center shadow-lg max-w-2xl mx-auto">
        <div class="bg-slate-50 w-24 h-24 rounded-full flex items-center justify-center mx-auto mb-6 text-slate-300 text-4xl">
          <i class="fas fa-archive"></i>
        </div>
        <h3 class="text-2xl font-bold text-slate-900 mb-2">The Archives are Empty</h3>
        <p class="text-slate-500 mb-8">No tournaments have been completed yet.</p>
        <a href="/tournaments" class="inline-flex items-center justify-center px-6 py-3 bg-brand-600 text-white font-bold rounded-xl hover:bg-brand-700 transition-all shadow-lg hover:shadow-brand-500/30">
          <i class="fas fa-search mr-2"></i> View Active Tournaments
        </a>
      </div>

    <% } else { %>
      
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        <% tournaments.forEach(t => { %>
          
          <div class="group bg-white border border-slate-200 rounded-2xl p-6 hover:shadow-xl transition-all duration-300 hover:-translate-y-1 relative overflow-hidden">
            
            <div class="absolute top-0 left-0 w-full h-1 bg-gradient-to-r from-slate-200 via-slate-400 to-slate-200 group-hover:from-brand-400 group-hover:via-brand-600 group-hover:to-brand-400 transition-all"></div>

            <div class="flex justify-between items-start mb-4">
              <span class="px-3 py-1 bg-slate-100 text-slate-600 rounded-lg text-xs font-bold uppercase tracking-wider">
                <%= t.game %>
              </span>
              <span class="text-xs font-bold text-slate-400 flex items-center">
                <i class="far fa-calendar-alt mr-1"></i> <%= t.endDate.toDateString() %>
              </span>
            </div>
            
            <h3 class="text-xl font-bold text-slate-900 mb-2 group-hover:text-brand-600 transition-colors">
              <%= t.name %>
            </h3>
            
            <p class="text-slate-500 text-sm mb-4 line-clamp-2">
              <%= t.description || "No description provided." %>
            </p>

            <% if (t.result && t.result.firstPlace) { %>
              <div class="bg-yellow-50 border border-yellow-100 rounded-xl p-3 mb-4 flex items-center gap-3">
                <div class="bg-yellow-100 text-yellow-600 w-10 h-10 rounded-full flex items-center justify-center flex-shrink-0">
                  <i class="fas fa-crown"></i>
                </div>
                <div class="overflow-hidden">
                  <p class="text-xs font-bold text-yellow-600 uppercase tracking-wide">Champion</p>
                  <p class="text-slate-900 font-bold truncate"><%= t.result.firstPlace.teamName %></p>
                </div>
              </div>
            <% } else { %>
              <div class="bg-slate-50 border border-slate-100 rounded-xl p-3 mb-4 text-center">
                <p class="text-slate-400 text-sm italic">Results pending archive...</p>
              </div>
            <% } %>

            <div class="flex items-center justify-between mt-auto pt-4 border-t border-slate-100">
              <div class="flex items-center text-xs text-slate-400">
                <i class="fas fa-user-shield mr-1.5"></i>
                <%= t.organizer?.username || "Admin" %>
              </div>
              
              <a href="/player/results/<%= t._id %>" class="text-sm font-bold text-slate-900 hover:text-brand-600 transition-colors flex items-center">
                View Results <i class="fas fa-arrow-right ml-1"></i>
              </a>
            </div>

          </div>
        <% }) %>
      </div>

    <% } %>
  </div>

  <div class="mt-auto">
    <%- include("../includes/footer") %>
  </div>

</body>
</html>