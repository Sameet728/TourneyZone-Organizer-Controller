<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>My Tournaments | BattleZone</title>
  
  <script src="https://cdn.tailwindcss.com"></script>
  
  <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />

  <script>
    tailwind.config = {
      theme: {
        extend: {
          fontFamily: {
            sans: ['"Plus Jakarta Sans"', 'sans-serif'],
          },
          colors: {
            brand: {
              50: '#eff6ff',
              100: '#dbeafe',
              500: '#3b82f6',
              600: '#2563eb', // Electric Blue
              900: '#1e3a8a',
            }
          }
        }
      }
    }
  </script>

  <style>
    /* Custom Scrollbar */
    ::-webkit-scrollbar { width: 8px; }
    ::-webkit-scrollbar-track { background: #f1f5f9; }
    ::-webkit-scrollbar-thumb { background: #cbd5e1; border-radius: 10px; }
    
    /* Soft Background Gradient Animation */
    @keyframes blob {
      0% { transform: translate(0px, 0px) scale(1); }
      33% { transform: translate(30px, -50px) scale(1.1); }
      66% { transform: translate(-20px, 20px) scale(0.9); }
      100% { transform: translate(0px, 0px) scale(1); }
    }
    .animate-blob { animation: blob 7s infinite; }
    .animation-delay-2000 { animation-delay: 2s; }
    .animation-delay-4000 { animation-delay: 4s; }
  </style>
</head>

<body class="bg-slate-50 text-slate-800 font-sans min-h-screen flex flex-col relative overflow-x-hidden">

  <div class="fixed top-0 left-0 w-full h-full overflow-hidden -z-10 pointer-events-none">
    <div class="absolute top-0 left-1/4 w-96 h-96 bg-purple-200 rounded-full mix-blend-multiply filter blur-3xl opacity-30 animate-blob"></div>
    <div class="absolute top-0 right-1/4 w-96 h-96 bg-blue-200 rounded-full mix-blend-multiply filter blur-3xl opacity-30 animate-blob animation-delay-2000"></div>
    <div class="absolute -bottom-32 left-1/3 w-96 h-96 bg-indigo-200 rounded-full mix-blend-multiply filter blur-3xl opacity-30 animate-blob animation-delay-4000"></div>
  </div>

  <div class="relative z-50">
    <%- include("../includes/navbar") %>
  </div>

  <div class="container mx-auto px-4 py-8 lg:py-12 flex-grow">
    
    <div class="flex flex-col md:flex-row justify-between items-center mb-10 gap-4">
      <div class="text-center md:text-left">
        <h2 class="text-3xl md:text-4xl font-extrabold text-slate-900 tracking-tight">
          My Tournaments <span class="text-2xl">⚡️</span>
        </h2>
        <p class="text-slate-500 mt-2 font-medium">Manage and track your gaming events</p>
      </div>

      <a href="/organizer/tournaments/new" 
         class="group relative inline-flex items-center justify-center px-8 py-3 text-base font-bold text-white transition-all duration-200 bg-brand-600 font-pj rounded-xl focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-brand-600 hover:bg-brand-900 shadow-lg hover:shadow-xl hover:-translate-y-1 w-full md:w-auto">
         <span class="mr-2 text-xl">🚀</span> Create New
         <div class="absolute inset-0 rounded-xl ring-2 ring-white/20 group-hover:ring-white/40 transition-all"></div>
      </a>
    </div>

    <% if (tournaments.length === 0) { %>
      
      <div class="bg-white/60 backdrop-blur-xl border border-white/20 rounded-3xl p-10 md:p-16 text-center shadow-xl max-w-2xl mx-auto">
        <div class="bg-indigo-50 w-24 h-24 rounded-full flex items-center justify-center mx-auto mb-6 shadow-inner">
          <i class="fas fa-gamepad text-4xl text-brand-600"></i>
        </div>
        <h3 class="text-2xl font-bold text-slate-900 mb-3">No Tournaments Found</h3>
        <p class="text-slate-500 mb-8 leading-relaxed">
          It looks quiet here! You haven't organized any battle arenas yet. <br class="hidden md:block">
          Start your journey by creating your first tournament.
        </p>
        <a href="/organizer/tournaments/new" class="inline-flex items-center justify-center px-6 py-3 text-sm font-bold text-brand-600 transition-all duration-200 bg-indigo-50 border border-transparent rounded-lg hover:bg-indigo-100 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-brand-600">
          <i class="fas fa-plus mr-2"></i> Create First Tournament
        </a>
      </div>

    <% } else { %>
      
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 lg:gap-8">
        
        <% tournaments.forEach(t => { %>
          <div class="group relative bg-white/80 backdrop-blur-sm rounded-2xl p-6 shadow-[0_8px_30px_rgb(0,0,0,0.04)] hover:shadow-[0_8px_30px_rgb(0,0,0,0.12)] border border-slate-100 transition-all duration-300 hover:-translate-y-1 flex flex-col h-full">
            
            <div class="flex justify-between items-start mb-4">
              <div class="flex items-center space-x-2">
                <span class="inline-flex items-center justify-center w-8 h-8 rounded-lg bg-slate-100 text-slate-600 group-hover:bg-brand-50 group-hover:text-brand-600 transition-colors">
                  <i class="fas fa-gamepad"></i>
                </span>
                <span class="font-bold text-sm text-slate-600 uppercase tracking-wide"><%= t.game %></span>
              </div>
              
              <span class="px-3 py-1 rounded-full text-xs font-bold uppercase tracking-wider border
                <%= t.status === 'upcoming' ? 'bg-amber-50 text-amber-600 border-amber-200' : '' %>
                <%= t.status === 'ongoing' ? 'bg-emerald-50 text-emerald-600 border-emerald-200' : '' %>
                <%= t.status === 'completed' ? 'bg-slate-100 text-slate-500 border-slate-200' : '' %>
              ">
                <%= t.status === 'upcoming' ? '⏳ ' : (t.status === 'ongoing' ? '🔥 ' : '🏁 ') %>
                <%= t.status %>
              </span>
            </div>

            <h5 class="text-xl font-bold text-slate-900 mb-2 leading-tight group-hover:text-brand-600 transition-colors">
              <%= t.name %>
            </h5>
            
            <p class="text-slate-500 text-sm mb-6 line-clamp-2 flex-grow">
              <%= t.description || "No description provided for this epic battle." %>
            </p>

            <div class="space-y-2 mb-6 text-sm font-medium text-slate-500 bg-slate-50/50 p-3 rounded-xl border border-slate-100">
              <div class="flex items-center">
                <i class="far fa-calendar-alt w-5 text-brand-500"></i>
                <span class="ml-2"><%= t.startDate.toDateString() %></span>
              </div>
              <div class="flex items-center">
                <i class="far fa-calendar-check w-5 text-brand-500"></i>
                <span class="ml-2"><%= t.endDate.toDateString() %></span>
              </div>
            </div>

            <div class="grid grid-cols-2 gap-3 mt-auto">
              <% if (t.status === 'completed') { %>
                <a href="/organizer/tournaments/<%= t._id %>/results" class="col-span-2 inline-flex justify-center items-center px-4 py-2.5 text-sm font-bold text-white transition-all duration-200 bg-emerald-500 rounded-lg hover:bg-emerald-600 hover:shadow-lg hover:shadow-emerald-500/30">
                  <i class="fas fa-trophy mr-2"></i> Submit Results
                </a>
              <% } else { %>
                <a href="/organizer/tournaments/<%= t._id %>/edit" class="inline-flex justify-center items-center px-4 py-2.5 text-sm font-bold text-white transition-all duration-200 bg-brand-600 rounded-lg hover:bg-brand-700 hover:shadow-lg hover:shadow-brand-500/30">
                  <i class="fas fa-edit mr-2"></i> Edit
                </a>
                <a href="/organizer/tournaments/<%= t._id %>" class="inline-flex justify-center items-center px-4 py-2.5 text-sm font-bold text-slate-700 transition-all duration-200 bg-white border border-slate-200 rounded-lg hover:bg-slate-50 hover:text-brand-600 hover:border-brand-200">
                  <i class="fas fa-eye mr-2"></i> View
                </a>
              <% } %>
            </div>

          </div>
        <% }) %>
      </div>
    <% } %>

  </div>

  <div class="mt-12">
    <%- include("../includes/footer") %>
  </div>

</body>
</html>