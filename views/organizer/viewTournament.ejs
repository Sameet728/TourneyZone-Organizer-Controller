<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= tournament.name %> | Dashboard</title>
  
  <script src="https://cdn.tailwindcss.com"></script>
  
  <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" />

  <script>
    tailwind.config = {
      theme: {
        extend: {
          fontFamily: {
            sans: ['"Plus Jakarta Sans"', 'sans-serif'],
          },
          colors: {
            brand: {
              500: '#3b82f6',
              600: '#2563eb',
              900: '#1e3a8a',
            }
          }
        }
      }
    }
  </script>

  <style>
    @keyframes blob {
      0% { transform: translate(0px, 0px) scale(1); }
      33% { transform: translate(30px, -50px) scale(1.1); }
      66% { transform: translate(-20px, 20px) scale(0.9); }
      100% { transform: translate(0px, 0px) scale(1); }
    }
    .animate-blob { animation: blob 7s infinite; }
    .animation-delay-2000 { animation-delay: 2s; }
  </style>
</head>

<body class="bg-slate-50 text-slate-800 font-sans min-h-screen flex flex-col relative">

  <div class="fixed top-0 left-0 w-full h-full overflow-hidden -z-10 pointer-events-none">
    <div class="absolute top-0 right-1/4 w-96 h-96 bg-blue-100 rounded-full mix-blend-multiply filter blur-3xl opacity-40 animate-blob"></div>
    <div class="absolute bottom-0 left-1/4 w-96 h-96 bg-indigo-100 rounded-full mix-blend-multiply filter blur-3xl opacity-40 animate-blob animation-delay-2000"></div>
  </div>

  <div class="relative z-50">
    <%- include("../includes/navbar") %>
  </div>

  <div class="container mx-auto px-4 py-8 lg:py-10 space-y-8 flex-grow">

    <div class="bg-white/80 backdrop-blur-xl border border-white/20 shadow-lg rounded-3xl p-6 md:p-8 relative overflow-hidden">
      <div class="relative z-10 flex flex-col md:flex-row justify-between items-start md:items-center gap-6">
        <div>
          <div class="flex items-center gap-3 mb-2">
            <span class="px-3 py-1 rounded-full text-xs font-bold uppercase tracking-wider bg-brand-100 text-brand-700">
              <%= tournament.game %>
            </span>
            <span class="px-3 py-1 rounded-full text-xs font-bold uppercase tracking-wider 
              <%= tournament.status === 'completed' ? 'bg-green-100 text-green-700' : 'bg-amber-100 text-amber-700' %>">
              <%= tournament.status %>
            </span>
          </div>
          <h2 class="text-3xl md:text-4xl font-extrabold text-slate-900 tracking-tight"><%= tournament.name %></h2>
        </div>
        
        <div class="flex items-center gap-4 bg-slate-50 p-4 rounded-xl border border-slate-100">
          <div class="text-center px-4 border-r border-slate-200">
            <p class="text-xs font-bold text-slate-400 uppercase">Entry Fee</p>
            <p class="text-xl font-extrabold text-slate-800">₹<%= tournament.entryFee %></p>
          </div>
          <div class="text-center px-4">
            <p class="text-xs font-bold text-slate-400 uppercase">Teams</p>
            <p class="text-xl font-extrabold text-slate-800"><%= tournament.teams.length %> <span class="text-slate-400 text-sm">/ <%= tournament.teamLimit %></span></p>
          </div>
        </div>
      </div>
      
      <div class="mt-6 text-slate-600 leading-relaxed border-t border-slate-100 pt-4">
        <strong>Description:</strong> <%= tournament.description || "No description provided." %>
      </div>
    </div>

    <% if (
       tournament.result &&
       tournament.result.firstPlace &&
       tournament.result.firstPlace.teamName
    ) { %>
      <div class="bg-gradient-to-br from-slate-900 to-slate-800 text-white rounded-3xl p-8 shadow-2xl relative overflow-hidden">
        <div class="absolute top-0 right-0 w-64 h-64 bg-yellow-500 rounded-full mix-blend-overlay filter blur-3xl opacity-20"></div>
        
        <h3 class="text-2xl font-bold mb-8 flex items-center">
          <i class="fas fa-trophy text-yellow-400 mr-3"></i> Tournament Hall of Fame
        </h3>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
          <div class="bg-white/10 backdrop-blur-sm border border-yellow-500/50 rounded-2xl p-6 text-center transform md:-translate-y-4 shadow-[0_0_30px_rgba(234,179,8,0.2)]">
            <div class="text-4xl mb-3">🥇</div>
            <div class="text-yellow-400 font-bold uppercase text-sm tracking-widest mb-1">Champion</div>
            <div class="text-xl font-bold text-white mb-1"><%= tournament.result.firstPlace.teamName %></div>
            <div class="text-sm text-slate-300">Leader: <%= tournament.result.firstPlace.leader %></div>
          </div>
          
          <div class="bg-white/5 backdrop-blur-sm border border-slate-400/30 rounded-2xl p-6 text-center">
            <div class="text-4xl mb-3">🥈</div>
            <div class="text-slate-300 font-bold uppercase text-sm tracking-widest mb-1">Runner Up</div>
            <div class="text-xl font-bold text-white mb-1"><%= tournament.result.secondPlace.teamName %></div>
            <div class="text-sm text-slate-400">Leader: <%= tournament.result.secondPlace.leader %></div>
          </div>

          <div class="bg-white/5 backdrop-blur-sm border border-orange-500/30 rounded-2xl p-6 text-center">
            <div class="text-4xl mb-3">🥉</div>
            <div class="text-orange-300 font-bold uppercase text-sm tracking-widest mb-1">Third Place</div>
            <div class="text-xl font-bold text-white mb-1"><%= tournament.result.thirdPlace.teamName %></div>
            <div class="text-sm text-slate-400">Leader: <%= tournament.result.thirdPlace.leader %></div>
          </div>
        </div>

        <% if (tournament.result.notes) { %>
          <div class="mt-8 pt-6 border-t border-white/10 text-slate-300 text-sm italic">
            <strong><i class="fas fa-quote-left mr-2"></i>Admin Notes:</strong> <%= tournament.result.notes %>
          </div>
        <% } %>
      </div>
    <% } %>

    <%
      var hasAcceptedPlayers =
        tournament.registrations &&
        tournament.registrations.filter(function (r) {
          return r.status === "accepted";
        }).length > 0;
    %>

    <% if (
      currentUser &&
      currentUser.role === "organizer" &&
      tournament.organizer._id.equals(currentUser._id) &&
      hasAcceptedPlayers
    ) { %>

      <div class="bg-white border border-slate-200 shadow-md rounded-2xl p-6 md:p-8">
        <div class="flex items-center gap-3 mb-6">
          <div class="p-3 bg-indigo-50 rounded-lg text-indigo-600">
            <i class="fas fa-key text-xl"></i>
          </div>
          <div>
            <h5 class="text-xl font-bold text-slate-900">Room Credentials</h5>
            <p class="text-slate-500 text-sm">Manage ID and Password sent to players</p>
          </div>
        </div>

        <% if (tournament.roomDetails && tournament.roomDetails.roomId) { %>
          <div class="bg-green-50 border border-green-200 rounded-xl p-4 mb-6 flex items-start gap-3">
            <i class="fas fa-check-circle text-green-600 mt-1"></i>
            <div>
              <p class="text-green-800 font-bold text-sm">Credentials Shared Successfully</p>
              <p class="text-green-700 text-xs mt-1">Last sent: <%= new Date(tournament.roomDetails.sharedAt).toLocaleString() %></p>
            </div>
          </div>
        <% } else { %>
          <div class="bg-blue-50 border border-blue-200 rounded-xl p-4 mb-6 flex items-start gap-3">
            <i class="fas fa-info-circle text-blue-600 mt-1"></i>
            <p class="text-blue-800 text-sm">Enter the Room ID and Password below. Clicking send will email all accepted players immediately.</p>
          </div>
        <% } %>

        <form action="/organizer/tournaments/<%= tournament._id %>/room" method="POST" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
          
          <div class="space-y-2">
            <label class="block text-xs font-bold uppercase text-slate-500 tracking-wider">Room ID</label>
            <input type="text" name="roomId" required
              value="<%= tournament.roomDetails && tournament.roomDetails.roomId ? tournament.roomDetails.roomId : '' %>"
              class="w-full px-4 py-3 bg-slate-50 border border-slate-200 rounded-xl focus:bg-white focus:ring-2 focus:ring-indigo-500 outline-none font-mono text-slate-800">
          </div>

          <div class="space-y-2">
            <label class="block text-xs font-bold uppercase text-slate-500 tracking-wider">Password</label>
            <input type="text" name="roomPassword" required
              value="<%= tournament.roomDetails && tournament.roomDetails.roomPassword ? tournament.roomDetails.roomPassword : '' %>"
              class="w-full px-4 py-3 bg-slate-50 border border-slate-200 rounded-xl focus:bg-white focus:ring-2 focus:ring-indigo-500 outline-none font-mono text-slate-800">
          </div>

          <div class="space-y-2">
            <label class="block text-xs font-bold uppercase text-slate-500 tracking-wider">Match Time</label>
            <input type="text" name="matchTime" placeholder="e.g. 6:30 PM" required
              value="<%= tournament.matchTime ? tournament.matchTime : '' %>"
              class="w-full px-4 py-3 bg-slate-50 border border-slate-200 rounded-xl focus:bg-white focus:ring-2 focus:ring-indigo-500 outline-none text-slate-800">
          </div>

          <div class="col-span-1 md:col-span-2 lg:col-span-3 pt-2">
            <button type="submit" class="w-full sm:w-auto inline-flex items-center justify-center px-6 py-3 bg-indigo-600 text-white font-bold rounded-xl hover:bg-indigo-700 transition-all shadow-lg shadow-indigo-200">
              <i class="fas fa-paper-plane mr-2"></i>
              <%= tournament.roomDetails && tournament.roomDetails.roomId ? "Update & Re-Send" : "Send Credentials" %>
            </button>
          </div>
        </form>
      </div>

    <% } %>

    <div class="bg-white border border-slate-200 shadow-sm rounded-2xl p-6">
      <h5 class="text-lg font-bold text-slate-900 mb-4 flex items-center">
        <i class="fas fa-users text-brand-500 mr-2"></i> Registered Teams
      </h5>
      
      <% if (tournament.teams.length === 0) { %>
        <p class="text-slate-500 italic">No teams have joined the lobby yet.</p>
      <% } else { %>
        <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
          <% tournament.teams.forEach(function (team) { %>
            <div class="bg-slate-50 p-4 rounded-xl border border-slate-100 flex items-center gap-3">
              <div class="w-10 h-10 rounded-full bg-white flex items-center justify-center text-slate-400 shadow-sm border border-slate-100">
                <i class="fas fa-shield-alt"></i>
              </div>
              <div class="overflow-hidden">
                <p class="font-bold text-slate-800 truncate"><%= team.name %></p>
                <p class="text-xs text-slate-500 truncate">Leader: <%= team.leader.username %></p>
              </div>
            </div>
          <% }) %>
        </div>
      <% } %>
    </div>

    <% if (
      currentUser &&
      currentUser.role === "organizer" &&
      tournament.organizer._id.equals(currentUser._id)
    ) { %>

      <div class="bg-white border border-slate-200 shadow-sm rounded-2xl p-6">
        <h5 class="text-lg font-bold text-slate-900 mb-6 flex items-center">
          <i class="fas fa-receipt text-green-500 mr-2"></i> Payment Verification
        </h5>

        <% if (!tournament.registrations || tournament.registrations.length === 0) { %>
          <div class="text-center py-10 bg-slate-50 rounded-xl border border-dashed border-slate-300">
            <p class="text-slate-400">No payment records found.</p>
          </div>
        <% } else { %>
          <div class="overflow-x-auto">
            <table class="w-full text-left border-collapse">
              <thead>
                <tr class="bg-slate-50 border-b border-slate-200">
                  <th class="px-4 py-3 text-xs font-bold text-slate-500 uppercase">Player</th>
                  <th class="px-4 py-3 text-xs font-bold text-slate-500 uppercase">Payer Name</th>
                  <th class="px-4 py-3 text-xs font-bold text-slate-500 uppercase">Amount</th>
                  <th class="px-4 py-3 text-xs font-bold text-slate-500 uppercase">UTR Ref</th>
                  <th class="px-4 py-3 text-xs font-bold text-slate-500 uppercase">Status</th>
                  <th class="px-4 py-3 text-xs font-bold text-slate-500 uppercase text-right">Actions</th>
                </tr>
              </thead>
              <tbody class="divide-y divide-slate-100">
                <% tournament.registrations.forEach(function (reg) { %>
                  <tr class="hover:bg-slate-50/50 transition-colors">
                    <td class="px-4 py-3 font-medium text-slate-700"><%= reg.user ? reg.user.username : "User" %></td>
                    <td class="px-4 py-3 text-slate-600"><%= reg.payerName %></td>
                    <td class="px-4 py-3 font-bold text-slate-700">₹<%= reg.amount %></td>
                    <td class="px-4 py-3">
                      <span class="font-mono text-xs bg-slate-100 px-2 py-1 rounded text-slate-600"><%= reg.transactionId %></span>
                    </td>
                    <td class="px-4 py-3">
                      <% if (reg.status === "pending") { %>
                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-amber-100 text-amber-800">Pending</span>
                      <% } else if (reg.status === "accepted") { %>
                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">Accepted</span>
                      <% } else { %>
                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-red-100 text-red-800">Rejected</span>
                      <% } %>
                    </td>
                    <td class="px-4 py-3 text-right space-x-2">
                      <% if (reg.status === "pending") { %>
                        <form action="/organizer/tournaments/<%= tournament._id %>/registrations/<%= reg._id %>/approve" method="POST" class="inline-block">
                          <button class="text-xs font-bold text-white bg-green-500 hover:bg-green-600 px-3 py-1.5 rounded-lg transition-colors">Accept</button>
                        </form>
                        <form action="/organizer/tournaments/<%= tournament._id %>/registrations/<%= reg._id %>/reject" method="POST" class="inline-block">
                          <input type="hidden" name="reason" value="Invalid UTR / Payment mismatch">
                          <button class="text-xs font-bold text-white bg-red-500 hover:bg-red-600 px-3 py-1.5 rounded-lg transition-colors">Reject</button>
                        </form>
                      <% } else { %>
                        <span class="text-slate-300">—</span>
                      <% } %>
                    </td>
                  </tr>
                <% }) %>
              </tbody>
            </table>
          </div>
        <% } %>
      </div>

    <% } %>

    <div class="pt-4">
      <a href="/organizer/tournaments" class="inline-flex items-center text-slate-500 hover:text-brand-600 font-bold transition-colors">
        <i class="fas fa-arrow-left mr-2"></i> Back to Tournaments
      </a>
    </div>

  </div>

  <div class="mt-auto">
    <%- include("../includes/footer") %>
  </div>

</body>
</html>