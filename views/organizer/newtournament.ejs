<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Create New Tournament | BattleZone</title>
  
  <script src="https://cdn.tailwindcss.com"></script>
  
  <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />

  <script>
    tailwind.config = {
      theme: {
        extend: {
          fontFamily: {
            sans: ['"Plus Jakarta Sans"', 'sans-serif'],
          },
          colors: {
            brand: {
              500: '#3b82f6',
              600: '#2563eb',
              900: '#1e3a8a',
            }
          }
        }
      }
    }
  </script>

  <style>
    /* Smooth fade in for the Time Slot field */
    .fade-enter {
      opacity: 0;
      transform: translateY(-10px);
      max-height: 0;
      overflow: hidden;
      transition: all 0.3s ease-out;
    }
    .fade-enter-active {
      opacity: 1;
      transform: translateY(0);
      max-height: 100px; /* Arbitrary large height */
    }
    
    /* Background Animation */
    @keyframes blob {
      0% { transform: translate(0px, 0px) scale(1); }
      33% { transform: translate(30px, -50px) scale(1.1); }
      66% { transform: translate(-20px, 20px) scale(0.9); }
      100% { transform: translate(0px, 0px) scale(1); }
    }
    .animate-blob { animation: blob 7s infinite; }
    .animation-delay-2000 { animation-delay: 2s; }
    .animation-delay-4000 { animation-delay: 4s; }
  </style>
</head>

<body class="bg-slate-50 text-slate-800 font-sans min-h-screen flex flex-col relative">

  <div class="fixed top-0 left-0 w-full h-full overflow-hidden -z-10 pointer-events-none">
    <div class="absolute top-0 left-1/4 w-96 h-96 bg-purple-200 rounded-full mix-blend-multiply filter blur-3xl opacity-30 animate-blob"></div>
    <div class="absolute top-0 right-1/4 w-96 h-96 bg-blue-200 rounded-full mix-blend-multiply filter blur-3xl opacity-30 animate-blob animation-delay-2000"></div>
    <div class="absolute -bottom-32 left-1/3 w-96 h-96 bg-indigo-200 rounded-full mix-blend-multiply filter blur-3xl opacity-30 animate-blob animation-delay-4000"></div>
  </div>

  <div class="relative z-50">
    <%- include("../includes/navbar") %>
  </div>

  <div class="container mx-auto px-4 py-8 lg:py-12 flex-grow flex items-center justify-center">
    
    <div class="w-full max-w-4xl relative">
      
      <div class="bg-white/80 backdrop-blur-xl border border-white/20 shadow-2xl rounded-3xl p-6 md:p-12">
        
        <div class="text-center mb-10">
          <div class="inline-flex items-center justify-center w-16 h-16 rounded-full bg-brand-50 text-brand-600 mb-4 shadow-sm">
            <i class="fas fa-trophy text-2xl"></i>
          </div>
          <h2 class="text-3xl md:text-4xl font-extrabold text-slate-900 tracking-tight">
            Create Tournament 🚀
          </h2>
          <p class="text-slate-500 mt-2">Setup the arena for the next big battle</p>
        </div>

        <div class="mb-8 bg-amber-50 border border-amber-200 rounded-xl p-4 flex items-start gap-4 shadow-sm">
          <div class="flex-shrink-0 text-amber-500 mt-1">
            <i class="fas fa-exclamation-triangle fa-lg"></i>
          </div>
          <div>
            <h4 class="text-sm font-bold text-amber-800 uppercase tracking-wide">Action Required</h4>
            <p class="text-sm text-amber-700 mt-1">
              Ensure your <b>UPI ID</b> is updated in your organizer profile. Players need this to pay entry fees and upload screenshots.
            </p>
          </div>
        </div>

        <form action="/organizer/tournaments" method="POST" class="space-y-6">
          
          <div class="grid grid-cols-1 md:grid-cols-2 gap-6 md:gap-8">
            
            <div class="space-y-2">
              <label for="name" class="block text-sm font-bold text-slate-700 ml-1">Tournament Name</label>
              <div class="relative">
                <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none text-slate-400">
                  <i class="fas fa-heading"></i>
                </div>
                <input type="text" name="name" id="name" placeholder="e.g. Winter Battle Royale" required 
                  class="w-full pl-11 pr-4 py-3 bg-slate-50 border border-slate-200 rounded-xl focus:bg-white focus:ring-2 focus:ring-brand-500 focus:border-transparent outline-none transition-all placeholder-slate-400 font-medium text-slate-700 hover:bg-slate-100/50">
              </div>
            </div>

            <div class="space-y-2">
              <label for="game" class="block text-sm font-bold text-slate-700 ml-1">Game Title</label>
              <div class="relative">
                <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none text-slate-400">
                  <i class="fas fa-gamepad"></i>
                </div>
                <input type="text" name="game" id="game" placeholder="e.g. BGMI, Valorant" required 
                  class="w-full pl-11 pr-4 py-3 bg-slate-50 border border-slate-200 rounded-xl focus:bg-white focus:ring-2 focus:ring-brand-500 focus:border-transparent outline-none transition-all placeholder-slate-400 font-medium text-slate-700 hover:bg-slate-100/50">
              </div>
            </div>

            <div class="space-y-2">
              <label for="entryFee" class="block text-sm font-bold text-slate-700 ml-1">Entry Fee (₹)</label>
              <div class="relative">
                <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none text-slate-400">
                  <i class="fas fa-rupee-sign"></i>
                </div>
                <input type="number" name="entryFee" id="entryFee" min="0" value="0" placeholder="0" required 
                  class="w-full pl-11 pr-4 py-3 bg-slate-50 border border-slate-200 rounded-xl focus:bg-white focus:ring-2 focus:ring-brand-500 focus:border-transparent outline-none transition-all placeholder-slate-400 font-medium text-slate-700 hover:bg-slate-100/50">
              </div>
            </div>

            <div class="space-y-2">
              <label for="teamLimit" class="block text-sm font-bold text-slate-700 ml-1">Team Slots</label>
              <div class="relative">
                <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none text-slate-400">
                  <i class="fas fa-users"></i>
                </div>
                <input type="number" name="teamLimit" placeholder="e.g. 100" required
                  class="w-full pl-11 pr-4 py-3 bg-slate-50 border border-slate-200 rounded-xl focus:bg-white focus:ring-2 focus:ring-brand-500 focus:border-transparent outline-none transition-all placeholder-slate-400 font-medium text-slate-700 hover:bg-slate-100/50">
              </div>
            </div>

          </div>

          <div class="space-y-2">
            <label for="description" class="block text-sm font-bold text-slate-700 ml-1">Rules & Description</label>
            <textarea name="description" id="description" rows="4" placeholder="Describe the format, maps, and rules..."
              class="w-full px-4 py-3 bg-slate-50 border border-slate-200 rounded-xl focus:bg-white focus:ring-2 focus:ring-brand-500 focus:border-transparent outline-none transition-all placeholder-slate-400 font-medium text-slate-700 hover:bg-slate-100/50"></textarea>
          </div>

          <div class="grid grid-cols-1 md:grid-cols-2 gap-6 md:gap-8">
            <div class="space-y-2">
              <label for="startDate" class="block text-sm font-bold text-slate-700 ml-1">Start Date</label>
              <div class="relative">
                 <input type="date" name="startDate" id="startDate" required 
                  class="w-full px-4 py-3 bg-slate-50 border border-slate-200 rounded-xl focus:bg-white focus:ring-2 focus:ring-brand-500 focus:border-transparent outline-none transition-all text-slate-700 font-medium">
              </div>
            </div>
            <div class="space-y-2">
              <label for="endDate" class="block text-sm font-bold text-slate-700 ml-1">End Date</label>
              <div class="relative">
                <input type="date" name="endDate" id="endDate" required 
                  class="w-full px-4 py-3 bg-slate-50 border border-slate-200 rounded-xl focus:bg-white focus:ring-2 focus:ring-brand-500 focus:border-transparent outline-none transition-all text-slate-700 font-medium">
              </div>
            </div>
          </div>

          <div class="grid grid-cols-1 md:grid-cols-2 gap-6 md:gap-8 items-start">
            
            <div class="space-y-2">
              <label for="type" class="block text-sm font-bold text-slate-700 ml-1">Format Type</label>
              <div class="relative">
                <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none text-slate-400">
                  <i class="fas fa-chess-board"></i>
                </div>
                <select name="type" id="type" required onchange="toggleTimeSlot()"
                  class="w-full pl-11 pr-10 py-3 bg-slate-50 border border-slate-200 rounded-xl focus:bg-white focus:ring-2 focus:ring-brand-500 focus:border-transparent outline-none transition-all text-slate-700 font-medium appearance-none cursor-pointer">
                  <option value="regular" selected>🏆 Regular Tournament</option>
                  <option value="scrim">⚔️ Daily Scrim</option>
                </select>
                <div class="absolute inset-y-0 right-0 pr-4 flex items-center pointer-events-none text-slate-400">
                  <i class="fas fa-chevron-down text-xs"></i>
                </div>
              </div>
            </div>

            <div id="timeSlotWrapper" class="space-y-2 fade-enter">
              <label for="timeSlot" class="block text-sm font-bold text-slate-700 ml-1">Daily Scrim Time</label>
              <div class="relative">
                <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none text-slate-400">
                  <i class="far fa-clock"></i>
                </div>
                <select name="timeSlot" id="timeSlot"
                  class="w-full pl-11 pr-10 py-3 bg-slate-50 border border-slate-200 rounded-xl focus:bg-white focus:ring-2 focus:ring-brand-500 focus:border-transparent outline-none transition-all text-slate-700 font-medium appearance-none cursor-pointer">
                  <option value="">-- Select Time Slot --</option>
                  <option value="9 AM - 12 PM">🌅 Morning (9 AM - 12 PM)</option>
                  <option value="12 PM - 3 PM">☀️ Noon (12 PM - 3 PM)</option>
                  <option value="3 PM - 6 PM">🌤 Afternoon (3 PM - 6 PM)</option>
                  <option value="6 PM - 9 PM">🌇 Evening (6 PM - 9 PM)</option>
                  <option value="9 PM - 12 AM">🌙 Night (9 PM - 12 AM)</option>
                </select>
                <div class="absolute inset-y-0 right-0 pr-4 flex items-center pointer-events-none text-slate-400">
                  <i class="fas fa-chevron-down text-xs"></i>
                </div>
              </div>
            </div>

          </div>

          <div class="pt-6">
            <button type="submit" 
              class="group relative w-full flex justify-center py-4 px-4 border border-transparent text-lg font-bold rounded-xl text-white bg-brand-600 hover:bg-brand-900 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-brand-500 transition-all duration-200 shadow-lg hover:shadow-brand-500/30 hover:-translate-y-1">
              <span class="absolute left-0 inset-y-0 flex items-center pl-3">
                <i class="fas fa-rocket text-brand-500 group-hover:text-white transition-colors"></i>
              </span>
              LAUNCH TOURNAMENT
            </button>
          </div>

        </form>
      </div>
    </div>
  </div>

  <div class="mt-auto">
    <%- include("../includes/footer") %>
  </div>

  <script>
    function toggleTimeSlot() {
      const type = document.getElementById("type").value;
      const wrapper = document.getElementById("timeSlotWrapper");
      const select = document.getElementById("timeSlot");

      if (type === "scrim") {
        wrapper.classList.add("fade-enter-active");
      } else {
        wrapper.classList.remove("fade-enter-active");
        select.value = ""; // Reset value
      }
    }

    // Trigger on load in case of browser auto-fill or back button navigation
    window.onload = toggleTimeSlot;
  </script>

</body>
</html>