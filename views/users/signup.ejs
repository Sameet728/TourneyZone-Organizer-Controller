<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Sign Up | TourneyZone</title>
  
  <script src="https://cdn.tailwindcss.com"></script>
  
  <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" />

  <script>
    tailwind.config = {
      theme: {
        extend: {
          fontFamily: {
            sans: ['"Plus Jakarta Sans"', 'sans-serif'],
          },
          colors: {
            brand: {
              500: '#3b82f6',
              600: '#2563eb',
              900: '#1e3a8a',
            }
          },
          animation: {
            'float': 'float 6s ease-in-out infinite',
            'slide-down': 'slideDown 0.3s ease-out forwards',
          },
          keyframes: {
            float: {
              '0%, 100%': { transform: 'translateY(0)' },
              '50%': { transform: 'translateY(-10px)' },
            },
            slideDown: {
              '0%': { opacity: '0', transform: 'translateY(-10px)', height: '0' },
              '100%': { opacity: '1', transform: 'translateY(0)', height: 'auto' },
            }
          }
        }
      }
    }
  </script>

  <style>
    @keyframes blob {
      0% { transform: translate(0px, 0px) scale(1); }
      33% { transform: translate(30px, -50px) scale(1.1); }
      66% { transform: translate(-20px, 20px) scale(0.9); }
      100% { transform: translate(0px, 0px) scale(1); }
    }
    .animate-blob { animation: blob 7s infinite; }
    .animation-delay-2000 { animation-delay: 2s; }
    
    .shake { animation: shake 0.5s cubic-bezier(.36,.07,.19,.97) both; }
    @keyframes shake {
      10%, 90% { transform: translate3d(-1px, 0, 0); }
      20%, 80% { transform: translate3d(2px, 0, 0); }
      30%, 50%, 70% { transform: translate3d(-4px, 0, 0); }
      40%, 60% { transform: translate3d(4px, 0, 0); }
    }

    /* Transition for UPI field visibility */
    #upiField {
      transition: all 0.3s ease-in-out;
    }
  </style>
</head>

<body class="bg-slate-50 text-slate-800 font-sans min-h-screen flex items-center justify-center relative overflow-x-hidden py-10">

  <div class="fixed top-0 left-0 w-full h-full overflow-hidden -z-10 pointer-events-none">
    <div class="absolute top-0 right-1/4 w-96 h-96 bg-brand-200 rounded-full mix-blend-multiply filter blur-3xl opacity-30 animate-blob"></div>
    <div class="absolute bottom-0 left-1/4 w-96 h-96 bg-purple-200 rounded-full mix-blend-multiply filter blur-3xl opacity-30 animate-blob animation-delay-2000"></div>
  </div>

  <div class="w-full max-w-lg px-4">
    
    <div class="bg-white/80 backdrop-blur-xl border border-white/50 shadow-2xl rounded-3xl p-8 md:p-10 relative overflow-hidden">
      
      <div class="absolute top-0 left-0 w-full h-2 bg-gradient-to-r from-purple-500 via-brand-600 to-brand-400"></div>

      <div class="text-center mb-8">
        <div class="inline-flex items-center justify-center w-16 h-16 bg-brand-50 text-brand-600 rounded-2xl mb-4 text-3xl shadow-sm animate-float">
          <i class="fas fa-user-plus"></i>
        </div>
        <h2 class="text-3xl font-extrabold text-slate-900 tracking-tight">Create Account</h2>
        <p class="text-slate-500 mt-2 text-sm">Join the arena and start your journey today.</p>
      </div>

      <form action="/signup" method="post" id="signupForm" class="space-y-5" novalidate>
        
        <div class="space-y-1">
          <label for="email" class="block text-sm font-bold text-slate-700 ml-1">Email Address</label>
          <div class="relative group">
            <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none text-slate-400 group-focus-within:text-brand-500 transition-colors">
              <i class="fas fa-envelope"></i>
            </div>
            <input type="email" name="email" id="email" placeholder="you@example.com" required
              class="w-full pl-11 pr-4 py-3.5 bg-slate-50 border border-slate-200 rounded-xl focus:bg-white focus:ring-2 focus:ring-brand-500 focus:border-transparent outline-none transition-all text-slate-800 font-medium placeholder-slate-400">
          </div>
        </div>

        <div class="space-y-1">
          <label for="username" class="block text-sm font-bold text-slate-700 ml-1">Gamer Tag / Username</label>
          <div class="relative group">
            <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none text-slate-400 group-focus-within:text-brand-500 transition-colors">
              <i class="fas fa-user-astronaut"></i>
            </div>
            <input type="text" name="username" id="username" placeholder="e.g. ShadowSlayer" required
              class="w-full pl-11 pr-4 py-3.5 bg-slate-50 border border-slate-200 rounded-xl focus:bg-white focus:ring-2 focus:ring-brand-500 focus:border-transparent outline-none transition-all text-slate-800 font-medium placeholder-slate-400">
          </div>
        </div>

        <div class="space-y-1">
          <label for="role" class="block text-sm font-bold text-slate-700 ml-1">I want to...</label>
          <div class="relative group">
            <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none text-slate-400 group-focus-within:text-brand-500 transition-colors">
              <i class="fas fa-compass"></i>
            </div>
            <select name="role" id="role" required
              class="w-full pl-11 pr-10 py-3.5 bg-slate-50 border border-slate-200 rounded-xl focus:bg-white focus:ring-2 focus:ring-brand-500 focus:border-transparent outline-none transition-all text-slate-800 font-medium appearance-none cursor-pointer">
              <option value="" disabled selected>Choose your path...</option>
              <option value="player">🎮 Compete as a Player</option>
              <option value="organizer">🏆 Host Tournaments (Organizer)</option>
            </select>
            <div class="absolute inset-y-0 right-0 pr-4 flex items-center pointer-events-none text-slate-400">
              <i class="fas fa-chevron-down text-xs"></i>
            </div>
          </div>
        </div>

        <div id="upiField" class="hidden overflow-hidden transition-all duration-300">
          <div class="bg-amber-50 border border-amber-100 rounded-xl p-4 space-y-2">
            <div class="flex items-center gap-2 text-amber-800 text-sm font-bold">
               <i class="fas fa-wallet"></i> Organizer Payment Details
            </div>
            <div class="relative group">
              <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none text-amber-400">
                <i class="fas fa-rupee-sign"></i>
              </div>
              <input type="text" name="upiId" id="upiId" placeholder="yourname@upi"
                class="w-full pl-11 pr-4 py-3 bg-white border border-amber-200 rounded-lg focus:ring-2 focus:ring-amber-500 outline-none transition-all text-slate-800 font-medium placeholder-slate-400">
            </div>
            <p class="text-xs text-amber-600 leading-tight">
              * Required to receive entry fees from players directly.
            </p>
          </div>
        </div>

        <div class="space-y-1">
          <label for="password" class="block text-sm font-bold text-slate-700 ml-1">Password</label>
          <div class="relative group">
            <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none text-slate-400 group-focus-within:text-brand-500 transition-colors">
              <i class="fas fa-lock"></i>
            </div>
            <input type="password" name="password" id="password" placeholder="Create a strong password" required minlength="8"
              class="w-full pl-11 pr-12 py-3.5 bg-slate-50 border border-slate-200 rounded-xl focus:bg-white focus:ring-2 focus:ring-brand-500 focus:border-transparent outline-none transition-all text-slate-800 font-medium placeholder-slate-400">
            
            <button type="button" onclick="togglePassword()" class="absolute inset-y-0 right-0 pr-4 flex items-center text-slate-400 hover:text-slate-600 cursor-pointer focus:outline-none">
              <i class="far fa-eye" id="toggleIcon"></i>
            </button>
          </div>
          
          <div class="h-1.5 w-full bg-slate-100 rounded-full mt-2 overflow-hidden">
             <div id="strengthMeter" class="h-full w-0 transition-all duration-300 bg-red-500"></div>
          </div>
          <p class="text-xs text-slate-400 text-right mt-1" id="strengthText">Min 8 chars</p>
        </div>

        <button type="submit" 
          class="w-full py-4 px-4 bg-brand-600 text-white font-bold rounded-xl shadow-lg hover:bg-brand-700 hover:shadow-brand-500/30 transform transition-all duration-200 hover:-translate-y-1 focus:ring-2 focus:ring-offset-2 focus:ring-brand-500 mt-2 group">
          CREATE ACCOUNT <i class="fas fa-arrow-right ml-2 group-hover:translate-x-1 transition-transform"></i>
        </button>

      </form>

      <div class="mt-8 text-center">
        <p class="text-slate-500 text-sm">
          Already have an account? 
          <a href="/login" class="font-bold text-brand-600 hover:text-brand-800 transition-colors underline decoration-2 decoration-brand-200 hover:decoration-brand-600">
            Log In here
          </a>
        </p>
      </div>

    </div>
  </div>

  <script>
    // --- 1. UPI Field Toggle Logic ---
    const roleSelect = document.getElementById('role');
    const upiField = document.getElementById('upiField');
    const upiInput = document.getElementById('upiId');

    roleSelect.addEventListener('change', function () {
      if (this.value === 'organizer') {
        upiField.classList.remove('hidden');
        // Add a small delay to allow display:block to apply before animating opacity/height if we were doing css animations
        // but tailwind 'hidden' simply toggles display:none. 
        // For a smoother slide effect, we can use a class toggle approach, but simple hidden works for functionality.
        upiField.classList.add('animate-slide-down'); 
        upiInput.setAttribute('required', 'required');
      } else {
        upiField.classList.add('hidden');
        upiField.classList.remove('animate-slide-down');
        upiInput.removeAttribute('required');
        upiInput.value = '';
      }
    });

    // --- 2. Password Toggle ---
    function togglePassword() {
      const passwordInput = document.getElementById('password');
      const icon = document.getElementById('toggleIcon');
      
      if (passwordInput.type === 'password') {
        passwordInput.type = 'text';
        icon.classList.remove('fa-eye');
        icon.classList.add('fa-eye-slash');
      } else {
        passwordInput.type = 'password';
        icon.classList.remove('fa-eye-slash');
        icon.classList.add('fa-eye');
      }
    }

    // --- 3. Password Strength Meter ---
    const passwordInput = document.getElementById('password');
    const strengthMeter = document.getElementById('strengthMeter');
    const strengthText = document.getElementById('strengthText');

    passwordInput.addEventListener('input', function() {
      const val = this.value;
      let strength = 0;
      
      if (val.length > 0) strength += 20;
      if (val.length > 5) strength += 20;
      if (val.length > 8) strength += 20;
      if (/[A-Z]/.test(val)) strength += 20;
      if (/[0-9]/.test(val)) strength += 20;

      strengthMeter.style.width = strength + '%';

      if (strength <= 40) {
        strengthMeter.className = 'h-full transition-all duration-300 bg-red-500';
        strengthText.innerText = 'Weak';
      } else if (strength <= 80) {
        strengthMeter.className = 'h-full transition-all duration-300 bg-yellow-400';
        strengthText.innerText = 'Medium';
      } else {
        strengthMeter.className = 'h-full transition-all duration-300 bg-green-500';
        strengthText.innerText = 'Strong';
      }
    });

    // --- 4. Form Validation Animation ---
    const form = document.getElementById('signupForm');
    form.addEventListener('submit', (e) => {
      if (!form.checkValidity()) {
        e.preventDefault();
        e.stopPropagation();
        
        const invalidInput = form.querySelector('input:invalid, select:invalid');
        if (invalidInput) {
          invalidInput.focus();
          // Find the parent container to shake
          const container = invalidInput.closest('.relative') || invalidInput.parentElement;
          container.classList.add('shake');
          setTimeout(() => container.classList.remove('shake'), 500);
        }
      }
      form.classList.add('was-validated');
    });
  </script>

</body>
</html>