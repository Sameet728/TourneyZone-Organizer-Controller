<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Player Dashboard</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <!-- Google Fonts for Gaming Vibe -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?&family=Oxanium:wght@400;600;700&display=swap" rel="stylesheet">
  <!-- Font Awesome for Icons -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" />

  <style>
    :root {
      --primary-glow: #00e5ff; /* Changed to cyan for players */
      --secondary-glow: #9f5dff; /* Changed to purple for players */
      --dark-bg: #111119;
      --card-bg: #1c1c27;
      --border-color: rgba(255, 255, 255, 0.1);
    }

    body {
      font-family: 'Oxanium', sans-serif;
      background-color: var(--dark-bg);
      color: #f0f0f0;
      background-image:
        radial-gradient(at 0% 0%, hsla(183, 100%, 35%, 0.3) 0px, transparent 50%),
        radial-gradient(at 98% 97%, hsla(253, 48%, 29%, 0.3) 0px, transparent 50%);
    }

    .page-header {
      margin-bottom: 3rem;
    }

    .page-header h2 {
      font-weight: 700;
      font-size: 2.5rem;
    }
    
    .page-header h2 .username {
        color: var(--primary-glow);
    }

    .action-card {
      background-color: var(--card-bg);
      border: 1px solid var(--border-color);
      border-radius: 0.75rem;
      text-align: center;
      padding: 2.5rem 1.5rem;
      transition: all 0.3s ease;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
    }

    .action-card:hover {
      transform: translateY(-8px);
      border-color: var(--primary-glow);
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
    }
    
    .card-icon {
        font-size: 4rem;
        color: var(--secondary-glow);
        margin-bottom: 1.5rem;
        text-shadow: 0 0 15px var(--secondary-glow);
        transition: color 0.3s ease;
    }

    .action-card:hover .card-icon {
        color: var(--primary-glow);
        text-shadow: 0 0 15px var(--primary-glow);
    }
    
    .action-card .card-title {
        font-weight: 700;
        font-size: 1.5rem;
        color: #fff;
    }

    .action-card .card-text {
        color: #aab;
        margin-bottom: 2rem;
    }
    
    .btn-glow {
      text-decoration: none;
      background-color: var(--primary-glow);
      border: none;
      color: #000; /* Dark text on cyan button */
      padding: 10px 25px;
      font-weight: 700;
      letter-spacing: 1px;
      text-transform: uppercase;
      border-radius: 6px;
      transition: all 0.3s ease;
    }

    .btn-glow:hover {
      background-color: #33ebff;
      box-shadow: 0 0 15px var(--primary-glow);
    }
  </style>
</head>

<body>
  <!-- Navbar -->
  <%- include("../includes/navbar") %>

  <!-- Page Content -->
  <div class="container my-5">
    <div class="page-header">
      <h2>Welcome, <span class="username"><%= currentUser.username %></span> 🎮</h2>
      <p class="text-white-50">Ready for the next challenge? Let's dive in!</p>
    </div>

    <div class="row g-4 justify-content-center">
      
      <!-- Browse Tournaments Card -->
      <div class="col-lg-4 col-md-6">
        <div class="action-card h-100">
          <div>
            <div class="card-icon"><i class="fas fa-search"></i></div>
            <h5 class="card-title">Browse Tournaments</h5>
            <p class="card-text">Explore upcoming tournaments and join the battle.</p>
          </div>
          <a href="/player/tournaments" class="btn btn-glow">Browse</a>
        </div>
      </div>

      <!-- My Tournaments Card -->
      <div class="col-lg-4 col-md-6">
        <div class="action-card h-100">
          <div>
            <div class="card-icon"><i class="fas fa-crosshairs"></i></div>
            <h5 class="card-title">My Tournaments</h5>
            <p class="card-text">View the tournaments you've joined and track their status.</p>
          </div>
          <a href="/player/my-tournaments" class="btn btn-glow">View Joined</a>
        </div>
      </div>

      <!-- View Results Card -->
      <div class="col-lg-4 col-md-6">
        <div class="action-card h-100">
          <div>
            <div class="card-icon"><i class="fas fa-trophy"></i></div>
            <h5 class="card-title">View Results</h5>
            <p class="card-text">Check the final results of completed tournaments.</p>
          </div>
          <a href="/player/results" class="btn btn-glow">Results</a>
        </div>
      </div>
      
    </div>
  </div>
  <%- include("../includes/footer") %>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>