<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Organizer Dashboard | TourneyZone</title>
  
  <script src="https://cdn.tailwindcss.com"></script>
  
  <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" />

  <script>
    tailwind.config = {
      theme: {
        extend: {
          fontFamily: {
            sans: ['"Plus Jakarta Sans"', 'sans-serif'],
          },
          colors: {
            brand: {
              50: '#eff6ff',
              100: '#dbeafe',
              500: '#3b82f6',
              600: '#2563eb',
              900: '#1e3a8a',
            }
          }
        }
      }
    }
  </script>

  <style>
    @keyframes blob {
      0% { transform: translate(0px, 0px) scale(1); }
      33% { transform: translate(30px, -50px) scale(1.1); }
      66% { transform: translate(-20px, 20px) scale(0.9); }
      100% { transform: translate(0px, 0px) scale(1); }
    }
    .animate-blob { animation: blob 7s infinite; }
    .animation-delay-2000 { animation-delay: 2s; }
  </style>
</head>

<body class="bg-slate-50 text-slate-800 font-sans min-h-screen flex flex-col relative">

  <div class="fixed top-0 left-0 w-full h-full overflow-hidden -z-10 pointer-events-none">
    <div class="absolute top-0 left-1/4 w-96 h-96 bg-purple-100 rounded-full mix-blend-multiply filter blur-3xl opacity-30 animate-blob"></div>
    <div class="absolute bottom-0 right-1/4 w-96 h-96 bg-brand-100 rounded-full mix-blend-multiply filter blur-3xl opacity-30 animate-blob animation-delay-2000"></div>
  </div>

  <div class="relative z-50">
    <%- include("../includes/navbar") %>
  </div>

  <div class="container mx-auto px-4 py-8 lg:py-12 flex-grow">
    
    <div class="flex flex-col md:flex-row justify-between items-center mb-12 gap-4">
      <div>
        <h1 class="text-3xl md:text-4xl font-extrabold text-slate-900 tracking-tight">
          Welcome back, <span class="text-brand-600"><%= currentUser.username %></span> 👋
        </h1>
        <p class="text-slate-500 mt-2 font-medium">Here is what’s happening with your tournaments today.</p>
      </div>
      
      <div class="hidden md:flex items-center gap-2 bg-white px-4 py-2 rounded-xl shadow-sm border border-slate-200 text-sm font-bold text-slate-500">
        <i class="far fa-calendar-alt text-brand-500"></i>
        <span><%= new Date().toLocaleDateString('en-US', { weekday: 'long', month: 'short', day: 'numeric' }) %></span>
      </div>
    </div>

    <div class="grid grid-cols-2 md:grid-cols-4 gap-4 mb-10">
      <div class="bg-white p-4 rounded-2xl border border-slate-100 shadow-sm flex flex-col items-center justify-center text-center">
        <div class="text-xs font-bold text-slate-400 uppercase tracking-wider mb-1">Status</div>
        <div class="text-green-500 font-extrabold text-lg flex items-center gap-1">
          <span class="w-2 h-2 bg-green-500 rounded-full animate-pulse"></span> Active
        </div>
      </div>
      <div class="bg-white p-4 rounded-2xl border border-slate-100 shadow-sm flex flex-col items-center justify-center text-center">
        <div class="text-xs font-bold text-slate-400 uppercase tracking-wider mb-1">Role</div>
        <div class="text-brand-600 font-extrabold text-lg">Organizer</div>
      </div>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-6 lg:gap-8 max-w-5xl mx-auto">
      
      <div class="group relative bg-white border border-slate-200 rounded-3xl p-8 shadow-lg hover:shadow-2xl hover:border-brand-200 transition-all duration-300 hover:-translate-y-1">
        <div class="absolute top-0 right-0 p-6 opacity-10 group-hover:opacity-20 transition-opacity">
          <i class="fas fa-plus-circle text-8xl text-brand-600 transform rotate-12"></i>
        </div>
        
        <div class="w-14 h-14 bg-brand-100 text-brand-600 rounded-2xl flex items-center justify-center text-2xl mb-6 shadow-inner">
          <i class="fas fa-rocket"></i>
        </div>
        
        <h3 class="text-2xl font-bold text-slate-900 mb-2">Host New Event</h3>
        <p class="text-slate-500 mb-8 leading-relaxed">
          Launch a new tournament. Set up the game, prize pool, entry fees, and schedule brackets.
        </p>
        
        <a href="/organizer/tournaments/new" class="inline-flex items-center justify-center w-full px-6 py-4 bg-brand-600 text-white font-bold rounded-xl hover:bg-brand-700 transition-all shadow-lg hover:shadow-brand-500/30">
          Create Tournament <i class="fas fa-arrow-right ml-2 group-hover:translate-x-1 transition-transform"></i>
        </a>
      </div>

      <div class="group relative bg-white border border-slate-200 rounded-3xl p-8 shadow-lg hover:shadow-2xl hover:border-purple-200 transition-all duration-300 hover:-translate-y-1">
        <div class="absolute top-0 right-0 p-6 opacity-10 group-hover:opacity-20 transition-opacity">
          <i class="fas fa-tasks text-8xl text-purple-600 transform -rotate-12"></i>
        </div>
        
        <div class="w-14 h-14 bg-purple-100 text-purple-600 rounded-2xl flex items-center justify-center text-2xl mb-6 shadow-inner">
          <i class="fas fa-clipboard-list"></i>
        </div>
        
        <h3 class="text-2xl font-bold text-slate-900 mb-2">Manage Events</h3>
        <p class="text-slate-500 mb-8 leading-relaxed">
          Track registrations, verify payments, update results, and manage your ongoing tournaments.
        </p>
        
        <a href="/organizer/tournaments" class="inline-flex items-center justify-center w-full px-6 py-4 bg-white text-slate-700 border-2 border-slate-200 font-bold rounded-xl hover:border-purple-500 hover:text-purple-600 transition-all">
          View Dashboard <i class="fas fa-external-link-alt ml-2"></i>
        </a>
      </div>

    </div>

    <div class="mt-12 text-center">
      <div class="inline-flex items-center gap-2 px-4 py-2 bg-yellow-50 text-yellow-700 rounded-full text-xs font-bold border border-yellow-200">
        <i class="fas fa-lightbulb"></i> Tip: Verify UPI payments before accepting teams into the bracket.
      </div>
    </div>

  </div>

  <div class="mt-auto">
    <%- include("../includes/footer") %>
  </div>

</body>
</html>